can use 10^3 as number of dimensions, was supposed to be number of points

to get points
a=rand(10^3,2);each line is one point
b=rand(10^3,2)

D(i,1)=sqrt((A(i,1)-B(i,1)).^2+(A(i,2)-B(i,2)).^2)


for reading: get overview
definitions
examples


for project:
dont need ssh login, just the email
